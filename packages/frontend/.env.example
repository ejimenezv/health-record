# API URL
VITE_API_URL=http://localhost:3001/api/v1

# WebSocket URL (future use)
VITE_WS_URL=http://localhost:3001

# Realtime streaming consultation mode (Prompt 27).
# When "true", the medical record page exposes a "Modo en Vivo" toggle that
# swaps the standard batch transcription panel for the live AI streaming view.
VITE_ENABLE_REALTIME=false
