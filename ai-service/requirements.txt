# ═══════════════════════════════════════════════════════════════
# MedRecord AI Service - Production Dependencies
# Versiones exactas para reproducibilidad (BSG requirement)
# ═══════════════════════════════════════════════════════════════

# ─── Web Framework ──────────────────────────────────────────────
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# ─── OpenAI / LLM ───────────────────────────────────────────────
# Pinned >=2.0 because chromadb 0.5.x pulls httpx 0.28 which dropped
# the legacy `proxies` kwarg that openai<2 relies on.
openai==2.33.0
tiktoken==0.5.2

# ─── RAG / Vector Store ─────────────────────────────────────────
# Match the chromadb server image (0.5.23). The 0.4.x client sends
# empty filter dicts that the 0.5 server rejects.
chromadb==0.5.23
langchain==0.1.6
langchain-openai==0.0.5
langchain-community==0.0.19

# ─── Document Processing ────────────────────────────────────────
pypdf==4.0.1
python-docx==1.1.0
unstructured==0.12.4

# ─── Audio Processing & Streaming (Real-Time) ──────────────────
pydub==0.25.1
opuslib==3.0.1
numpy==1.26.4
silero-vad==5.1.2
torch==2.2.0
torchaudio==2.2.0
# Speaker embedding model for the audio-feature diarizer. Pretrained
# weights bundled in the wheel (no HF auth needed). Used to detect when
# the same vs different person is talking, replacing the keyword-only
# heuristic for speaker-id assignment.
resemblyzer==0.1.4
librosa==0.10.1

# ─── Security ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
slowapi==0.1.9

# ─── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# ─── Redis (Session State & Cache - CRITICAL for Real-Time) ─────
redis==5.0.1
aioredis==2.0.1

# ─── HTTP Client ────────────────────────────────────────────────
httpx==0.25.2
aiohttp==3.9.3

# ─── Observability ──────────────────────────────────────────────
langfuse==2.7.3
prometheus-client==0.19.0
structlog==24.1.0

# ─── Utilities ──────────────────────────────────────────────────
python-multipart==0.0.7
python-dotenv==1.0.1
tenacity==8.2.3
